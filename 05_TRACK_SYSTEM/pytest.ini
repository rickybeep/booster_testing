[pytest]
testpaths = tests
addopts = -ra -m "not hardware and not live_motion"
markers =
    hardware: requires separately authorized hardware access and is skipped by default
    live_motion: can command physical motion and must never run without explicit approval
